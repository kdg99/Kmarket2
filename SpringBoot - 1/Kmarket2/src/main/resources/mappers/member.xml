<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.Kmarket2.dao.UserDAO">
	
	<!-- <select id="selectTerms" resultType="kr.co.Kmarket2.vo.TermsVO">
		select * from `km_member_terms`;
	</select> -->
	
	<!-- <insert id="insertUser">
		insert into `km_member` set
			`uid`=#{uid}, 
			`pass`=#{pass}, 
			`name`=#{name}, 
			`nick`=#{nick}, 
			`email`=#{email}, 
			`hp`=#{hp}, 
			`zip`=#{zip}, 
			`addr1`=#{addr1}, 
			`addr2`=#{addr2}, 
			`regip`=#{regip},
			`rdate`=NOW(); 
	</insert> -->
</mapper>